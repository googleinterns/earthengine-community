/**
 * Every template will have root panel with id panel-template-0.
 */
export const ROOT_ID = 'panel-template-0';

/**
 * Root URL for all API endpoints.
 */
export const API_URL = 'https://ee-app-creator.appspot.com';

/**
 * URL route for templates endpoint.
 */
export const templatesRoute = '/api/v1/templates';

/**
 * Template key for widget reference.
 */
export const WIDGET_REF = 'widgetRef';

/**
 * List of default map styles.
 */
export const MAP_STYLES = [
  'standard',
  'silver',
  'retro',
  'night',
  'dark',
  'aubergine',
];
